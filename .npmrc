# Hoist native/problematic packages from workspace deps to root node_modules
# Required for Astro to build without incorporating dependencies from workspace packages
public-hoist-pattern[]=keyv
public-hoist-pattern[]=@keyv/*
