import { ephemera } from '@/lib/collections/ephemera/config';
import { images } from '@/lib/collections/images/config';
import { locations } from '@/lib/collections/locations/config';
import { pages } from '@/lib/collections/pages/config';
import { posts } from '@/lib/collections/posts/config';
import { regions } from '@/lib/collections/regions/config';
import { series } from '@/lib/collections/series/config';
import { themes } from '@/lib/collections/themes/config';

export const collections = {
	ephemera,
	images,
	locations,
	pages,
	posts,
	regions,
	series,
	themes,
};
