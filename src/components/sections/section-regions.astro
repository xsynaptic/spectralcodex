---
import type { HTMLAttributes } from 'astro/types';

import ContentSection from '#components/content/content-section.astro';
import RegionsList from '#components/regions/regions-list.astro';
import { createRegionsByIdsFunction } from '#lib/collections/regions/regions-utils.ts';
import { getTranslations } from '#lib/i18n/i18n-translations.ts';

interface Props extends HTMLAttributes<'div'> {
	regionIds: Array<string> | undefined;
}

const { regionIds, ...props } = Astro.props;

const getRegionsByIds = await createRegionsByIdsFunction();

const regions = regionIds ? getRegionsByIds(regionIds) : undefined;

const t = getTranslations();
---

<ContentSection
	sectionTitle={t('collection.regions.labelPlural')}
	data-pagefind-ignore
	data-nosnippet
	{...props}
>
	<RegionsList
		terms={regions}
		dividerContent="chevron"
		showMicroformats={true}
		showSingleRegionAncestors={true}
	/>
</ContentSection>
