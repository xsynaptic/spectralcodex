---
import type { HTMLAttributes } from 'astro/types';

import type { CatalogItem } from '#lib/catalog/catalog-types.ts';

import ContentSection from '#components/content/content-section.astro';
import PreviewList from '#components/parts/preview-list.astro';
import Prose from '#components/parts/prose.astro';
import { getTranslations } from '#lib/i18n/i18n-translations.ts';

interface Props extends HTMLAttributes<'div'> {
	count: number;
	items: Array<CatalogItem>;
}

const { items, count, ...props } = Astro.props;

const t = getTranslations();
---

{items.length > 0 ? (
	<ContentSection
		data-nosnippet
		data-pagefind-ignore
		sectionTitle={t('section.itemsList')}
		{...props}
	>
		<Prose>
			<PreviewList count={count} items={items} />
		</Prose>
	</ContentSection>
) : undefined}
