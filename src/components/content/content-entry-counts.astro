---
import DividedItem from '#components/parts/divided-item.astro';
import { backlinksDisplayCountMinimum } from '#constants.ts';
import { getTranslations } from '#lib/i18n/i18n-translations.ts';

interface Props {
	entryCount: number;
	backlinksCount: number;
}

const { entryCount, backlinksCount } = Astro.props;

const t = getTranslations();
---

{entryCount > 0 ? (
	<DividedItem>{t.plural('content.meta.entries.label', entryCount)}</DividedItem>
) : undefined}
{backlinksCount >= backlinksDisplayCountMinimum ? (
	<DividedItem>{t.plural('content.meta.backlinks.label', backlinksCount)}</DividedItem>
) : undefined}
