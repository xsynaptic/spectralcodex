---
import { ReactSearchComponent } from '@spectralcodex/react-search-component';

const bundlePath = `${import.meta.env.BASE_URL}pagefind/`;
---

<ReactSearchComponent
	client:load
	bundlePath={bundlePath}
	pageSize={10}
	ranking={{ pageLength: 0.5, termFrequency: 0.8 }}
/>
