---
import { openGraphImageHeight, openGraphImageWidth } from '@spectralcodex/shared/constants';
import { SEO } from 'astro-seo';

import type { MetaProps } from '#components/types.ts';

import { openGraphImageDensity, openGraphTwitterUsername } from '#constants.ts';
import { getTranslations } from '#lib/i18n/i18n-translations.ts';
import { getSeoImageFallback } from '#lib/utils/seo.ts';
import { sanitizeImageAltAttribute } from '#lib/utils/text.ts';

type Props = MetaProps;

const {
	title: contentTitle,
	description: contentDescription,
	ogType,
	image,
	imageAlt,
	article,
	prefetchUrls,
	noIndex,
	noFollow,
} = Astro.props;

const t = getTranslations();

const siteTitle = t('site.title');
const siteDescription = t('site.description');

const description = contentDescription ?? siteDescription;

const titleDisplay =
	contentTitle && contentTitle !== siteTitle ? `${contentTitle} - ${siteTitle}` : siteTitle;

function generateOpenGraphImageUrl({
	imageUrl,
	imageAlt,
}: {
	imageUrl?: string | URL | undefined;
	imageAlt: string;
}) {
	const imageLocalUrl = imageUrl ? new URL(imageUrl, Astro.site).href : undefined;

	const openGraphImageUrl = imageLocalUrl ?? getSeoImageFallback();

	return {
		url: openGraphImageUrl,
		height: openGraphImageHeight * openGraphImageDensity,
		width: openGraphImageWidth * openGraphImageDensity,
		alt: sanitizeImageAltAttribute(imageAlt),
	};
}

const openGraphImage = generateOpenGraphImageUrl({
	imageUrl: image?.url,
	imageAlt: image?.alt ?? imageAlt ?? description,
});
---

<SEO
	title={titleDisplay}
	description={description}
	openGraph={{
		basic: {
			title: titleDisplay,
			type: ogType ?? 'website',
			image: openGraphImage.url,
		},
		optional: {
			description,
			locale: 'en_US',
			siteName: siteTitle,
		},
		image: openGraphImage,
		...(ogType === 'article' && article !== undefined ? { article } : {}),
	}}
	twitter={{
		card: 'summary_large_image',
		site: openGraphTwitterUsername,
		title: contentTitle ?? siteTitle,
		description,
		image: openGraphImage.url,
		imageAlt: openGraphImage.alt,
	}}
	extend={{
		...(prefetchUrls?.length
			? {
					link: prefetchUrls.map((prefetchUrl) => ({ rel: 'prefetch', href: prefetchUrl })),
				}
			: {}),
		meta: [{ name: 'generator', content: Astro.generator }],
	}}
	noindex={noIndex ?? false}
	nofollow={noFollow ?? false}
	robotsExtras="max-snippet:-1, max-image-preview:large, max-video-preview:-1"
/>
