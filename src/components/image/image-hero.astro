---
import type { ImageComponentProps, ImagePlaceholderProps } from '#lib/image/image-types.ts';

import ImageHeroFrame from '#components/image/image-hero-frame.astro';
import ImagePlaceholder from '#components/image/image-placeholder.astro';
import Image from '#components/parts/image.astro';
import { getImageByIdFunction } from '#lib/collections/images/images-utils.ts';
import { getImageBreakpoints } from '#lib/image/image-layout.ts';
import { sanitizeAltAttribute } from '#lib/utils/text.ts';

interface Props {
	hasHeroImage: boolean;
	imageId: string | undefined;
	alt: string;
	class?: string | undefined;
}

const { hasHeroImage, imageId, alt, class: className } = Astro.props;

async function getImageProps(
	imageId: string | undefined,
	alt: string
): Promise<{
	imageProps: ImageComponentProps | undefined;
	placeholderProps: ImagePlaceholderProps | undefined;
}> {
	const getImageById = await getImageByIdFunction();

	const imageEntry = getImageById(imageId);

	if (!hasHeroImage || !imageId || !imageEntry)
		return { imageProps: undefined, placeholderProps: undefined };

	return {
		imageProps: {
			src: imageId,
			breakpoints: getImageBreakpoints({ maxWidth: imageEntry.data.width }),
			sizes: '100vw',
			priority: true,
			unstyled: true,
			alt: sanitizeAltAttribute(alt),
		} satisfies ImageComponentProps,
		placeholderProps: {
			imageId,
			aspectRatio: imageEntry.data.width / imageEntry.data.height,
			highQuality: true,
		},
	};
}

const { imageProps, placeholderProps } = await getImageProps(imageId, alt);
---

{
	hasHeroImage ? (
		<ImageHeroFrame class:list={['overflow-hidden', className]}>
			<ImagePlaceholder placeholderProps={placeholderProps}>
				{imageProps ? (
					<Image
						class:list={[
							'z-10 max-h-screen min-h-[400px] text-sm text-transparent xl:min-h-screen',
							'aspect-3/2 sm:aspect-5/4 md:aspect-3/2 lg:aspect-16/9 xl:aspect-auto',
						]}
						data-pagefind-meta="image[src], image_alt[alt]"
						{...imageProps}
					/>
				) : (
					<div
						class:list={[
							'max-h-screen min-h-[400px] w-full text-sm text-transparent xl:min-h-screen',
							'aspect-3/2 sm:aspect-5/4 md:aspect-3/2 lg:aspect-16/9 xl:aspect-auto',
						]}
					/>
				)}
			</ImagePlaceholder>
			<slot />
		</ImageHeroFrame>
	) : (
		<slot />
	)
}
