---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'div'> {
	status: 'notice' | 'warning' | 'error';
	class?: string | undefined;
}

const { status, class: className, ...props } = Astro.props;
---

<div
	class:list={[
		'rounded-sm border px-2 py-1 text-sm',
		status === 'notice'
			? 'border-primary-300 bg-primary-100 text-primary-600 dark:border-primary-900 dark:bg-primary-600 dark:text-primary-200'
			: undefined,
		status === 'warning'
			? 'border-amber-300 bg-amber-100 text-amber-600 dark:border-amber-900 dark:bg-amber-600 dark:text-amber-200'
			: undefined,
		status === 'error'
			? 'border-orange-300 bg-orange-100 text-orange-600 dark:border-orange-900 dark:bg-orange-600 dark:text-orange-200'
			: undefined,
		className,
	]}
	{...props}
>
	<slot />
</div>
