---
import { FEATURE_DATE_ARCHIVES } from '#constants.ts';
import TimelineIndex from '#layouts/timeline-index.astro';
import { getTimelineData } from '#lib/timeline/timeline-data.ts';

const { timelineIndexData, timelineYears } = await getTimelineData();

const data = {
	timelineIndexData,
	timelineYears,
};
---

{FEATURE_DATE_ARCHIVES ? <TimelineIndex {...data} /> : undefined}
