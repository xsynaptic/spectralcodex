---
import type { GetStaticPaths, InferGetStaticPropsType } from 'astro';

import RegionEntry from '#layouts/region-entry.astro';
import { getRegionsCollection } from '#lib/collections/regions/regions-data.ts';
import { createQueryRegionsEntryFunction } from '#lib/collections/regions/regions-utils.ts';

export const getStaticPaths = (async ({ paginate }) => {
	const { entries } = await getRegionsCollection();

	const queryRegionsEntry = await createQueryRegionsEntryFunction();

	const results = [];

	for (const entry of entries) {
		const { catalogItemsFiltered, mapData, catalogItems, catalogItemsCount, regionsOption } =
			queryRegionsEntry(entry);

		results.push(
			...paginate(catalogItemsFiltered, {
				params: { id: entry.id },
				props: {
					entry,
					mapData,
					catalogItems,
					catalogItemsCount,
					regionsOption,
				},
				pageSize: 20,
			}),
		);
	}

	return results;
}) satisfies GetStaticPaths;

type Props = InferGetStaticPropsType<typeof getStaticPaths>;
---

<RegionEntry {...Astro.props} />
