# Server Configuration
STORAGE_PATH=/mnt/volume_xxx_01
DEPLOY_REMOTE_PATH=/var/www/html

# Image server secret for URL signing (generate with: openssl rand -hex 32)
IPX_SERVER_SECRET=

# Umami secrets (generate with: openssl rand -hex 32)
UMAMI_DB_PASSWORD=
UMAMI_APP_SECRET=
