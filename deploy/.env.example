# Static site path
DEPLOY_SITE_PATH=/var/www/project

# Project storage root (contains /media and /cache subdirectories)
DEPLOY_MEDIA_PATH=/mnt/storage/project

# Image server secret (must match IPX_SERVER_SECRET in root .env)
IPX_SERVER_SECRET=
